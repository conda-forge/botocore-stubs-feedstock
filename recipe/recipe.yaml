schema_version: 1

context:
  name: botocore-stubs
  version: "1.42.12"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 0eaac8852441794f14bdb823b0b0dc1c7220cf024e506031d5e0d2c4855e277f

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - setuptools
    - python ${{ python_min }}.*
  run:
    - python >=${{ python_min }}
    - types-awscrt
    - typing_extensions >=4.1.0

tests:
  - requirements:
      run:
        - pip
        - python ${{ python_min }}
    script:
      - python -c "import importlib; importlib.import_module('botocore-stubs')"
      - pip check

about:
  summary: Type annotations for botocore.
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/vemel/mypy_boto3_builder
  repository: https://github.com/vemel/mypy_boto3_builder/tree/main/types_botocore

extra:
  recipe-maintainers:
    - thewchan
